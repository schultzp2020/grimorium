import { it, expect } from 'vitest'

// Recluse's only feature is the "misregister" perception modifier effect.
// The perception behavior is tested in Misregister.test.ts.
it("Recluse's perception behavior is tested in Misregister.test.ts", () => {
  expect(true)
})
