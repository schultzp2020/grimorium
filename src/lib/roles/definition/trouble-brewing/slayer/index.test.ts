import { it, expect } from 'vitest'

// Slayer's only feature is the "slayer_bullet" day action effect.
// The day action condition and behavior are tested in SlayerBullet.test.ts.
it("Slayer's day action is tested in SlayerBullet.test.ts", () => {
  expect(true)
})
