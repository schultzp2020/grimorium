import { it, expect } from 'vitest'

// Scarlet Woman is a passive role — all behavior is on the demon_successor effect.
// See src/lib/effects/definition/demon-successor/index.test.ts for handler tests.
it('Scarlet Woman has no testable features (behavior is on effect)', () => {
  expect(true)
})
