import { it, expect } from 'vitest'

// Saint's only feature is the "martyrdom" win condition effect.
// The win condition behavior is tested in Martyrdom.test.ts.
it("Saint's win condition is tested in Martyrdom.test.ts", () => {
  expect(true)
})
